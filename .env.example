# Local env files live per app — copy and edit:
#   cp apps/api/.env.example apps/api/.env
#   cp apps/dashboard/.env.example apps/dashboard/.env
#
# The repo root .env is not loaded by the API or dashboard.
