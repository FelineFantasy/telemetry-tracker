import { ComingSoonPage } from "@/app/components/dashboard/ComingSoonPage";

export default function LogsPage() {
  return (
    <ComingSoonPage
      title="Logs"
      description="Structured log search, live tail, and correlation with traces and errors — coming in a future release."
    />
  );
}
