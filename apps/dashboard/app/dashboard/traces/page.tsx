import { ComingSoonPage } from "@/app/components/dashboard/ComingSoonPage";

export default function TracesPage() {
  return (
    <ComingSoonPage
      title="Traces"
      description="Distributed tracing across services — span timelines, flame graphs, and trace search will live here."
    />
  );
}
