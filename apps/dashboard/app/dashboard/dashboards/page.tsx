import { ComingSoonPage } from "@/app/components/dashboard/ComingSoonPage";

export default function DashboardsPage() {
  return (
    <ComingSoonPage
      title="Dashboards"
      description="Custom charts and saved views — build dashboards from events, errors, and sessions."
    />
  );
}
